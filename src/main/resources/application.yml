server:
  port: 8080
spring:
  application:
    name: cpfirst
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cpfirst?useTimezone=true&serverTimezone=UTC
    username: root
    password: Lipe@123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
    open-in-view: false
logging:
  level:
    root: WARN
    #com.example.cpfirst: DEBUG
